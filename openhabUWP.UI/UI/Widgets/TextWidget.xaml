<UserControl
    x:Class="openhabUWP.UI.Widgets.TextWidget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:openhabUWP.Widgets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance local:TextWidget, IsDesignTimeCreatable=True}"
    x:Name="control">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- image -->

        <!-- text -->
        <TextBlock
            Grid.Column="1"
            Text="{Binding ElementName=control, Path=Label}"
            TextWrapping="Wrap"
            VerticalAlignment="Center"
            MaxLines="2"
            TextTrimming="CharacterEllipsis"/>

        <!-- value -->
        <TextBlock
            Grid.Column="2"
            Text="{Binding ElementName=control, Path=Value}"
            TextWrapping="NoWrap"
            VerticalAlignment="Center"
            MaxLines="1"
            Margin="0,0,10,0"
            Visibility="{Binding ElementName=control, Path=Value, Converter={StaticResource StringToVisibilityConverter}}"/>

    </Grid>
</UserControl>
